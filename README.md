# Phaser Game

## Static Assets Folder

It's in the public folder by default. You can configure it in package.json.

```json
"staticFiles": {
    "staticPath": "public",
    "watcherGlob": "**"
  }
```
